[package]
name = "snapfzz-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core contracts: manifests, lifecycle, bus types, plugin traits"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
