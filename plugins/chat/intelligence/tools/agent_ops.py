"""Agent management tools — inter-agent communication.

Extracted from: agentscope-ai/QwenPaw/src/qwenpaw/agents/tools/

Tools:
    list_agents()                     — enumerate available agents
    chat_with_agent(agent_id, msg)    — peer-to-peer agent communication via HTTP
    get_token_usage()                 — current token consumption
    get_current_time()                — timezone-aware current time
    set_user_timezone(tz)             — set IANA timezone
    create_memory_search_tool(...)    — semantic search on conversation memory
"""

# TODO: Extract agent ops tools from QwenPaw repo
