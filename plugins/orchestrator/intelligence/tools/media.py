"""Media tools — screenshot, image, video.

Extracted from: agentscope-ai/QwenPaw/src/qwenpaw/agents/tools/

Tools:
    desktop_screenshot()    — capture screen
    view_image(path)        — display image to agent
    view_video(path)        — display video to agent
    send_file_to_user(path) — FileBlock format delivery
"""

# TODO: Extract media tools from QwenPaw repo
