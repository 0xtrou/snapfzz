# Snapfzz Intelligence Runtime — Python dependencies
# Extracted from QwenPaw + Snapfzz-specific additions

# Core — AgentScope framework
agentscope>=1.0.18
agentscope-runtime>=1.1.3

# Memory — ReMeLight (default backend)
remelight>=0.1.0

# Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# Tools
playwright>=1.40.0             # browser_use tool

# Phase 6 — PostgreSQL memory (uncomment when ready)
# psycopg2-binary>=2.9.0
# pgvector>=0.3.0
# sqlalchemy>=2.0.0
