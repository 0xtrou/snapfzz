[project]
name = "snapfzz-intelligence"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "agentscope-runtime",
    "agentscope",
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "pytest-asyncio",
    "httpx",
    "fakeredis",
]
