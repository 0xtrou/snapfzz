[project]
name = "snapfzz-intelligence"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "agentscope",
    "fastapi",
    "uvicorn[standard]",
    "sse-starlette",
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "pytest-asyncio",
    "httpx",
]
